{
  "name": "docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "predeploy": "npm run build",
    "build": "npm run build-project1 && npm run build-project2 && npm run build-project3",
    "build-project1": "cd react-masterclass && npm install && npm run build",
    "build-project2": "cd movie-app && npm install && npm run build",
    "build-project3": "cd cultural-event-info && npm install && npm run build",
    "deploy": "gh-pages -d build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
